# HEX SDK
#
# No program is built here. The test is a shell script driving python against
# the checked-in cyborg patch, so this Makefile exists only to put the
# directory on the SUBDIRS list ../Makefile builds from globbing.

include ../../../../build.mk

include $(HEX_MAKEDIR)/hex_sdk.mk
